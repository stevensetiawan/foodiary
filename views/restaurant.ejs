<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Diary</title>
    <style>
        th, td{
            padding: 10px 10px;
        }
    </style>
</head>
<body>
    <h1>Foods</h1>
    <table border ="1">
        <tr>
            <th>Restaurant Name</th>
            <th>Address</th>
            <th>Phone Number</th>
            <th>Action</th>
        </tr>
        <% for(let i = 0; i < data.length; i++) { %>
            <tr>
                <td><%= data[i].name%></td>
                <td><%= data[i].address%></td>
                <td><%= data[i].phone%></td>
                <td><button><a href="/retaurants/edit/<%= data[i].id %>">Edit</a></button> <button><a href="/restaurants/delete/<%= data[i].id %>">delete</a></button></td>
            </tr>
        <% } %>
    </table>
</body>
</html>